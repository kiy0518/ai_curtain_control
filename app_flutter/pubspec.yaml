name: ai_curtain_app
description: AI Curtain — BLE remote control (Flutter, Material 3)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
